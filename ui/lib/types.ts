export interface Bot {
  name: string;
  bio: string;
  personality: string;
  starting_dialogue: string;
  ticker_symbol: string;
  contract_address: string;
  ticker: string;
  creator: string;
  twitter?: string;
  image?: string;
} 